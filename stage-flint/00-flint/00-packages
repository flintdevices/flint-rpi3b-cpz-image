python3
python3-pip
python3-venv
python3-pygame
python3-pil
libsdl2-dev
libsdl2-image-dev
libsdl2-ttf-dev
libasound2-dev
device-tree-compiler
i2c-tools
# provides con2fbmap, used by flint-panel-console.service to route a text
# console onto the ST7789 SPI panel early in boot (see 01-run.sh section 7)
fbset
# required by APPLaunch itself (not just LaunchWizard) — without it the
# dynamic linker fails to exec the binary at all (libinput.so.10 not found)
libinput10
